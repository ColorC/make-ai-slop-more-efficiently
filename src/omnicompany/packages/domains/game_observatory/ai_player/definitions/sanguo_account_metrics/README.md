# 三国账号关键数值直接观测定义

这些 definition 只允许用于 `omni game player account metric-derive` 的局部截图 OCR。

约束：

- 数值必须在同一 EvidenceStep 的 canonical Before/After 原图中直接可见。
- region 只框住一个完整、无缩写的整数，不接受整屏 OCR 或 Agent 文字总结。
- OCR 置信度必须至少 0.95；Before/After 可相同，用于记录稳定的当前读数。
- 如果游戏界面只显示“24.3万”等压缩值，必须继续打开详情页取得完整整数；不能把近似值登记为精确读数。
