# 独立判读修正

- 受影响文件：`prechange-run/adjudication.json`
- 原结论：冻结截图没有直接显示右侧“语音”文字。
- 复核结论：在原始 1080 × 1920 图像中，“语音”文字清楚可见。原结论来自错误的缩放阅读，不能继续作为负例依据。
- 处理：保留原文件作为当时判读记录；后续评分采用 `postchange-retry-run/adjudication.json` 中的原尺寸复核结果。既有 prechange 分数不用于晋级证明。
- 仍未解决：左下可见箭头的 `tap` 行为尚无直接执行证据；fixture 中已验证的是系统 `back` 动作。此样本保留为动作语义歧义回归样本。